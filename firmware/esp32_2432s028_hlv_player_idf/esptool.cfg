[esptool]
# Tested with the board's CH340C on Windows after adding capacitance from EN
# to GND.  Hold reset for 500 ms, switch DTR first and RTS immediately after
# it, then keep GPIO0 low for a reliable 50 ms before returning to idle.
custom_reset_sequence = D0|R1|W0.500|D1|R0|W0.050|D0|R0
